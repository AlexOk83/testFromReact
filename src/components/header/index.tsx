import React from "react";
import './style.sass'

function Header () {
    return (
        <header className="header">
            <div className="logo" />
        </header>
    );
}

export default Header;